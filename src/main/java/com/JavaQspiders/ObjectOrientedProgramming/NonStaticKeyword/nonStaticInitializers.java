package com.JavaQspiders.ObjectOrientedProgramming.NonStaticKeyword;

public class nonStaticInitializers {

    /*
    Single Line non-static Initializers : Any variable which is declared and initialized and not prefixed with static is called SLNSI
    Multi Line non-static Initializers(Anonymous Block) : Any block of code which is not prefixed with anything is called as non-static initializers (Non-static context area)
    execute first and execute one time
     */
    //slnsi
    String name = null;
    int num = 69;

    //mlnsi
    {
        //Non-static context area
    }
}
